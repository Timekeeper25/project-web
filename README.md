langkah ambil project laravel di github

+ import database

+ buka command line prompt di folder
    - git clone https://github.com/Timekeeper25/project-web

+ set environment
    - composer install
    - php artisan key:generate
    - php artisan storage:link
